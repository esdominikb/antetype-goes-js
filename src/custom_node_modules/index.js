'use strict';

var path = require('path');
var fs = require('fs');


module.exports = function(){
    var pathToAntetypeDev = path.join(process.env.HOME, "/Library/Application Support/com.antetype.Antetype-DEV");
    var antetypeScriptsFolder = path.join(pathToAntetypeDev, '/Scripts');

    try{
        if(fs.lstatSync(antetypeScriptsFolder).isDirectory()){
            //console.log("ok");
        }

    }catch(e){
        console.log(e);
    }

    return {
        pathToAntetypeScripts: antetypeScriptsFolder
    };
};




